import SectionTitle from "../../../Componets/SectionTitle/SectionTitle";


const CreateTask = () => {
    return (
        <div>
           <SectionTitle subHeading={'For the user'} heading={'New Task'}></SectionTitle>
           
        </div>
    );
};

export default CreateTask;